# {{ ansible_managed }}
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
host    all             all             10.11.12.0/24           md5

{% for host in groups['webservers'] %}
host    all       {{ deploy_user }}	    {{ hostvars[host]['ansible_ssh_host'] }}/32           md5 # {{ host }}
{% endfor %}
